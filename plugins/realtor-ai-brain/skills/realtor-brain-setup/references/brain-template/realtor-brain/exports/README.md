# Exports — session staging only (your real documents live in your workspace)

This local folder is just **temporary staging while a document is being built**. It is wiped when the
session ends — nothing durable lives here.

**Your finished documents live in your cloud workspace** (Google Drive or OneDrive), in the folder each
one belongs to — your AI Brain doc and Business Plan in `01 · AI Brain`, guides in `03 · Content/Guides`,
market reports in `05 · Market`, listing content in `04 · Listings`. Ask me for any document and I'll
fetch it or tell you exactly where it is.

These are polished *outputs*, not the source of truth — the canonical data lives in the Brain's
`identity/` and `memory/` files, so any document can always be regenerated.
