# Exports
**Rendered views only — not source of truth.** Safe to delete; skills regenerate them.

Things that land here:
- The brand persona / knowledge `.docx` (human-readable render of `identity/`)
- Offer guidebooks, lead magnets
- Generated dashboards
- Market-update reports, listing kits

The canonical data always lives in `identity/` and `memory/`. These are just polished outputs.
